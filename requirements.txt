# Astra — Core dependencies
# Licensed under Apache License 2.0

# ── Required ─────────────────────────────────────────────────────────────────
numpy>=1.24.0
grpcio>=1.60.0
grpcio-tools>=1.60.0          # for proto compilation only
protobuf>=4.25.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.26.0
psutil>=5.9.0
cryptography>=41.0.0          # for TEE seal/unseal (AES-GCM) and TLS
cffi>=1.16.0                  # cryptography Rust backend dependency

# ── Optional: GPU inference ───────────────────────────────────────────────────
# torch>=2.2.0                 # install from https://pytorch.org
# torchvision>=0.17.0

# ── Required for GPU inference: KTransformers heterogeneous C++ kernels ───────
# Do NOT pip install ktransformers directly. Build from source with:
#     bash scripts/build_ktransformers.sh
# See docs/INSTALL.md for full GPU setup instructions.

# ── Optional: P2P DHT node discovery (Phase 3) ───────────────────────────────
# hivemind>=1.1.0              # pip install hivemind

# ── Development ──────────────────────────────────────────────────────────────
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
# black>=24.0.0
# ruff>=0.3.0
# mypy>=1.9.0
